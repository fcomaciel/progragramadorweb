// Número (Number)
let numero = 120;

// Estringe (String)
let texto = 'String';

// Booleano (Booleans)
const booleano = true;

// Vetor (Array)
let vetor = [1, 2, 3, 4, 5, 6, 7, 8];

// Objeto (Object)
let objeto = {
    propriedade: 12,
    propriedade2: 'teste'
};

// Função (Funtion)
let umaFuncao = function () {
    // fazer algo
};

function outraFuncao () {
    umaFuncao();
    umaFuncao();
}
